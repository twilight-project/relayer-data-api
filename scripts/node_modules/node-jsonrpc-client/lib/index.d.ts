import type { CookieJar } from 'tough-cookie';
export declare class JsonRpcError implements Error {
    code: string;
    name: string;
    message: string;
    stack?: string;
    constructor(message: string, code: string);
}
export declare type JsonRpcResult<T> = {
    id: string;
    jsonrpc: '2.0';
    result?: T;
    error?: Error;
};
export declare class JsonRpc {
    private url;
    constructor(url: string);
    setUrl(url: string): void;
    call<P, R>(method: string, params: P, cookieJar?: CookieJar): Promise<JsonRpcResult<R>>;
}
